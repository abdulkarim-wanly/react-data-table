/**
 * Side-effect / bootstrap entry: import this path when your bundler drops
 * `setDefaultDataTableFiltersHost` from the main package chunk (e.g. stale Vite optimize cache).
 *
 * @example
 * import { setDefaultDataTableFiltersHost } from 'genesis-react-data-table/setup';
 */
export { setDefaultDataTableFiltersHost } from './components/InlineFiltersUI/InlineFiltersUI';
